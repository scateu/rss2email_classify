# Always preview first
    python imap_classifier.py --dry-run

# See every UID → folder mapping
    python imap_classifier.py --dry-run --verbose

# Execute
    python imap_classifier.py


# TODO
 - [ ] 不如直接改rss2email的原始配置，给配置里加map
